sudo: required
services:
  - docker

before_install:
  - docker build -t aruprakshit/react-app-dockerized -f Procfile.dev .

script:
  - docker run aruprakshit/react-app-dockerized npm run test -- --coverage
